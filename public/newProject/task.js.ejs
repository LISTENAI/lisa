const lisa = require('@listenai/lisa_core')
module.exports = ({job, fs, ...core} = lisa) => {
  job('<%= name %>:demo', {
    title: 'demo任务',
    task: async () => {
      await fs.remove('./out')
      fs.project.root = './out'
      await fs.project.mkdir('app', 'config', 'target')
    },
  })
}

